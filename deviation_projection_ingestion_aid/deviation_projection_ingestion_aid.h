#ifndef __DEVIATION_PROJECTION_INGESTION_AID_H__
#define __DEVIATION_PROJECTION_INGESTION_AID_H__

#include <stdint.h>
#include <stdlib.h>
#include <memory>
#include <internal_rest/network_rest_frame.h>
#include <deviation_projection_client/deviation_projection_client.h>
#include "model.h"

namespace deviation_projection_ingestion_aid
{
    class ClientTrainingDataIngestionMaster
    {
        private:

        public:

            auto set_client_remote()
            {

            }

            auto set_data_source()
            {

            }

            auto set_cancellation_token()
            {

            }

            auto set_per_sink_token_per_second()
            {

            }

            auto run()
            {

            }
            // auto set_client_retry_policy()
            // {

            // }

            // auto set_connection_config()
            // {

            // }
    };
}

#endif